from django.conf.urls import patterns, url

urlpatterns = patterns(
    'views',

    # url(r'^event/?$',
    #     event_views.EventListCreateView.as_view(),
    #     name='event_api'
    #     ),
    # url(r'^event/(?P<pk>\d+)/?$',
    #     event_views.EventDetail.as_view(),
    #     name='event_api'
    #     ),

)
